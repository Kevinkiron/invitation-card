node_modules
.next
.env.local
.env
.DS_Store
*.log

# Scratch folder used when files had to be moved aside rather than deleted
# (the desktop bridge cannot remove files). Safe to delete by hand.
_to_delete/

# Downloaded at build/dev time by scripts/fetch-demo-photos.mjs and
# scripts/fetch-greeting-photos.mjs — other people's photographs, several
# megabytes each, fetched fresh rather than committed as binaries.
public/demo/
public/greetings/
